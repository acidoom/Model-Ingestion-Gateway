"""OPA promotion transport — lazy-imported only when ``--opa`` is selected.

Driving the host ``opa`` binary needs no Python dependency (a marker extra,
``mig[opa]``), so the default promotion path stays stdlib-only (I10).
"""
